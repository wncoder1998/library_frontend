# AGENTS.md

## 项目概览

- 本项目是基于 Vite、React 和 TypeScript 构建的前端应用。
- 源码位于 `src/` 目录。
- 静态资源位于 `public/` 目录。
- 构建配置和工具配置位于项目根目录。

## 技术栈

- react
- 路由: react-router-dom
- 样式: tailwindcss
- ui库: antd

## 常用命令

- 安装依赖：`pnpm install`
- 启动开发服务器：`pnpm dev`
- 构建生产产物：`pnpm build`
- 运行代码检查：`pnpm lint`
- 预览生产构建：`pnpm preview`

## 工作准则

- 优先进行小而聚焦的修改，并保持与现有项目结构一致。
- 修改前先阅读相关上下文代码，遵循项目中已经存在的实现模式。
- 不要在任务型修改中混入无关重构。
- 除非用户明确要求，不要删除或覆盖用户已有修改。
- 优先使用明确的 TypeScript 类型，除非有充分理由，否则避免使用 `any`。
- 保持界面在常见屏幕尺寸下可访问、可响应。

## 代码风格

- 以现有 ESLint 配置作为代码风格的准则。
- 优先使用函数式 React 组件和 Hooks。
- 保持组件职责清晰；只有在能减少实际重复或复杂度时才抽取辅助函数。
- 为组件、属性、Hooks 和路由相关文件使用清晰的命名。
- 避免为显而易见的代码添加注释；只在说明非显而易见的行为或约束时添加简短注释。
- 尽可能的使用技术栈中的技术来实现
- 图片没有的时候，用ai生成，不要用svg图片
- 样式代码和组件代码要尽可能的分离

## 测试与验证

完成有实际影响的代码修改前，应运行最相关的可用检查：

- `pnpm lint`
- `pnpm build`

如果某个命令无法运行，需要说明原因，并描述仍然存在的风险。

## 依赖管理

- 除非请求的修改确实需要，否则不要新增运行时依赖。
- 在可行时优先使用项目已有依赖和浏览器或平台原生 API。
- 如果必须新增依赖，需要说明原因，并保持修改范围受控。

## Git 与安全

- 在进行大范围修改前，先检查工作区状态。
- 未经用户明确允许，不要运行 `git reset --hard`、`git checkout --` 等破坏性 Git 命令。
- 保留与当前任务无关的已修改文件。
- 生成文件、锁文件变更和格式化改动应限制在任务必要范围内。
